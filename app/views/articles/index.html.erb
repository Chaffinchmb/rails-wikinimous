<div class="container">

  <div class="row justify-content-center">
    <div class="col-6">
      <% @articles.each do |article| %>
        <ul>
          <li><%= link_to article.title, article_path(article), class: "titles" %></li>
        </ul>
      <% end %>
    </div>
  </div>

</div>
